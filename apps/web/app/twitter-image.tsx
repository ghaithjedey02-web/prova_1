/**
 * X/Twitter card: the same build-time OG image, so every platform that
 * prefers twitter:image over og:image still gets the brand card.
 */
export { default, alt, size, contentType } from './opengraph-image';
